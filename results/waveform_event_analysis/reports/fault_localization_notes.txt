Preliminary separability analysis only. Each bus currently has one SLG case and one three-phase case, so leave-one-out nearest-neighbor localization can rely on the opposite fault type at the same bus as the only same-bus reference. Reported exact/top-3/1-hop scores should therefore be interpreted as optimistic bus-fingerprint separability, not a fully independent localization benchmark.
